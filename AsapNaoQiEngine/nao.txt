IETS MET HET TIME MANAGEMENT -- JE WEET NU NIET WAT 0..1 IS? HOE WEET JE DE DURATION A PRIORI?

NO CONFLICT MANAGEMENT YET // you cannot start two copies in parallel, but you CAN start two different clips simultaneously.

JNaoQiEngine:
De planner
-- verwacht JNaoQiEmbodiment
-- maakt daarvoor een clipcacher
-- en een binding

JNaoQiUnit
--PlayCachedClipNQU
	planunit that plays a cached clip through the naoqiclipcacher. When initted it will check if clip available. When started, it will run start the next available copy of the clip.
--SayNQU
	planunit that says something

NaoQiClipCacher

gegeven een behaviormanagerproxy, vraag op welke behaviors beschikbaar zijn en zorg dat er zoveel mogelijk ge pre-cached worden zodat het spelen va clips niet tot een delay leidt.

In: proxy
        ALBehaviorManagerProxy bp = nqe.getBehaviorManagerProxy();
		ClipListener (gets a callback when clip finishes playing)

Init: 
	vraag alle clips op bij proxy
        String[] ibs = bp.getInstalledBehaviors(); (debug msg which clips!)
	de clips worden gesorteerd in een map met multiple values (arraylist als value):
		key-value: (name without numerical suffix - list of all clip names that are same apart from possible numerical suffix)
	Hiervan worden 3 versies gemaakt: preloaded -- running -- tobepreloaded
	
Functionality:
	getAnimatinoNames 
		(returns keyset of available animation names (without numerical suffix)
	
	animationExists (string animationname)
		true iff we have one or more copies of a clip under that name
		
	startNextAvailable(String anomatinoName) 
		starts the next available one, blocking until one is available and no copy of this clip running (but not until it finished playing!)
		internally, full clip names are taken from preloaded, moved to running, moved to tobepreloaded, moved to preloaded
		Note± when a clip runs, we should not be able to start other copies of same clip. But when it finished but is not yet preloaded, we should be able to start one already.