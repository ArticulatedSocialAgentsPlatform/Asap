/**
 * Manages world objects that are sent from an external rendering environment through ipaaca.
 * This is used to get information on which objects are there in an external rendering environment, and where they are located,
 * so that a virtual character can, for example, gaze or point at them.
 */
@hmi.util.NoEmptyClassWarning
package asap.ipaacaworldenvironment;